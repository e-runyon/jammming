
const client_id = '';
const client_secret = '';
let accessToken;
const endpointURI = 'https://accounts.spotify.com/api/token';

const Spotify = {
    getAccessToken() {
        if (accessToken) {
            return accessToken;
        }

        const new
    }
}

export default Spotify;